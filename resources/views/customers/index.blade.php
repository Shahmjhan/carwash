@extends('layouts.app') @section('content')<div class="page-head"><div><h1>Customers</h1><p>CRM and complete customer history.</p></div><a class="primary" href="{{ route('customers.create') }}">+ New Customer</a></div><form class="search"><input name="q" placeholder="Search name or phone" value="{{ request('q') }}"><button>Search</button></form><div class="panel"><table><thead><tr><th>Customer</th><th>Phone</th><th>Vehicles</th><th>Visits</th><th>Lifetime Spend</th><th></th></tr></thead><tbody>@forelse($customers as $c)<tr><td><a href="{{ route('customers.show',$c) }}"><b>{{ $c->full_name }}</b></a><small>{{ $c->customer_code }}</small></td><td>{{ $c->phone }}</td><td>{{ $c->vehicles->count() }}</td><td>{{ $c->total_visits }}</td><td>Rs. {{ number_format($c->total_spending,2) }}</td><td><a href="{{ route('customers.show',$c) }}">View →</a></td></tr>@empty<tr><td colspan="6" class="empty">No customers found.</td></tr>@endforelse</tbody></table>{{ $customers->links() }}</div>@endsection
