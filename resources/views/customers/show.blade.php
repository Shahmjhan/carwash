@extends('layouts.app') @section('content')<div class="page-head"><div><h1>{{ $customer->full_name }}</h1><p>{{ $customer->customer_code }} · {{ $customer->phone }}</p></div><a class="primary" href="{{ route('vehicles.create',['customer_id'=>$customer->id]) }}">+ Add Vehicle</a></div><div class="stats"><div><small>Vehicles</small><b>{{ $customer->vehicles->count() }}</b></div><div><small>Visits</small><b>{{ $customer->total_visits }}</b></div><div><small>Loyalty</small><b>{{ number_format($customer->loyalty_points,0) }} pts</b></div><div><small>Lifetime Value</small><b>Rs. {{ number_format($customer->total_spending,2) }}</b></div></div><div class="grid2"><section class="panel"><h2>Vehicles</h2>@forelse($customer->vehicles as $v)<a class="listrow" href="{{ route('vehicles.show',$v) }}"><b>{{ $v->registration_number }}</b><span>{{ $v->make }} {{ $v->model }} · {{ $v->category }}</span></a>@empty<p class="empty">No vehicles.</p>@endforelse</section><section class="panel"><h2>Recent Jobs</h2>@forelse($customer->jobs->sortByDesc('created_at')->take(8) as $j)<a class="listrow" href="{{ route('jobs.show',$j) }}"><b>{{ $j->job_number }}</b><span>{{ $j->vehicle->registration_number }} · <span class="badge">{{ $j->status->getLabel() }}</span></span></a>@empty<p class="empty">No service history.</p>@endforelse</section></div>@endsection
