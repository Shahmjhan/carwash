@extends('layouts.app') @section('content')<div class="page-head"><div><h1>Job Cards</h1><p>Track every vehicle visit from check-in to delivery.</p></div><div><a class="secondary" href="{{ route('jobs.board') }}">Live Board</a> <a class="primary" href="{{ route('jobs.create') }}">+ New Job</a></div></div><div class="panel"><table><thead><tr><th>Job</th><th>Vehicle</th><th>Customer</th><th>Status</th><th>Priority</th><th>Technician</th><th></th></tr></thead><tbody>@foreach($jobs as $j)<tr><td><a href="{{ route('jobs.show',$j) }}"><b>{{ $j->job_number }}</b></a></td><td>{{ $j->vehicle->registration_number }}</td><td>{{ $j->customer->full_name }}</td><td><span class="badge">{{ $j->status->getLabel() }}</span></td><td>{{ ucfirst($j->priority) }}</td><td>{{ optional($j->technician)->name ?? 'Unassigned' }}</td><td><a href="{{ route('jobs.show',$j) }}">Open →</a></td></tr>@endforeach</tbody></table>{{ $jobs->links() }}</div>@endsection
