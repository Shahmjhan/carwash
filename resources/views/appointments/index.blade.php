@extends('layouts.app') @section('content')<div class="page-head"><div><h1>Appointments</h1><p>Scheduled visits, reschedules and cancellations.</p></div><a class="primary" href="{{ route('appointments.create') }}">+ Appointment</a></div><div class="panel"><table><thead><tr><th>Date/time</th><th>Customer</th><th>Vehicle</th><th>Status</th><th></th></tr></thead><tbody>@foreach($appointments as $a)<tr><td>{{ $a->scheduled_at->format('d M Y H:i') }}</td><td>{{ $a->customer->full_name }}</td><td>{{ $a->vehicle->registration_number }}</td><td><span class="badge">{{ $a->status }}</span></td><td><a href="{{ route('appointments.edit',$a) }}">Edit</a></td></tr>@endforeach</tbody></table>{{ $appointments->links() }}</div>@endsection
