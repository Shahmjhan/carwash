@extends('layouts.app') @section('content')<div class="page-head"><h1>New Appointment</h1></div><div class="panel form-panel"><form method="post" action="{{ route('appointments.store') }}">@csrf<div class="form-grid"><label>Customer<select name="customer_id">@foreach($customers as $c)<option value="{{ $c->id }}">{{ $c->full_name }} — {{ $c->phone }}</option>@endforeach</select></label><label>Vehicle<select name="vehicle_id">@foreach($vehicles as $v)<option value="{{ $v->id }}">{{ $v->registration_number }} — {{ $v->make }} {{ $v->model }}</option>@endforeach</select></label><label>Date & time<input type="datetime-local" name="scheduled_at" required></label><label class="wide">Notes<textarea name="notes"></textarea></label></div><button class="primary">Create Appointment</button></form></div>@endsection
