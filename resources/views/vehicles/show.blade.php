@extends('layouts.app') @section('content')<div class="page-head"><div><h1>{{ $vehicle->registration_number }}</h1><p>{{ $vehicle->make }} {{ $vehicle->model }} · {{ $vehicle->category }}</p></div><a class="primary" href="{{ route('jobs.create',['vehicle_id'=>$vehicle->id]) }}">+ Create Job</a></div><div class="grid2"><section class="panel"><h2>Vehicle details</h2><div class="details"><span>Customer</span><b>{{ $vehicle->customer->full_name }}</b><span>Mileage</span><b>{{ number_format($vehicle->mileage) }} km</b><span>Fuel</span><b>{{ $vehicle->fuel_level ?? '—' }}%</b><span>VIN</span><b>{{ $vehicle->vin ?? '—' }}</b></div></section><section class="panel"><h2>Service timeline</h2>@forelse($vehicle->jobs->sortByDesc('created_at') as $j)<a class="listrow" href="{{ route('jobs.show',$j) }}"><b>{{ $j->job_number }}</b><span>{{ $j->created_at->format('d M Y') }} · {{ $j->status->getLabel() }}</span></a>@empty<p class="empty">No service history.</p>@endforelse</section></div>@endsection
